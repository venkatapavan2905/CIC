apiVersion: v1
kind: Deployemnt
metadata:
  name: spring-boot-app
  labels:
    app: spring-boot-app
spec:
  replicas: 2
  selector:
    matchLables:
      app: spring-boot-app
  template:
    metadata:
      labels:
        app: spring-boot-app
    spec:
      containers:
      - name: spring-boot-app
        image: venkatapavan29/cicd-pipeline:replaceImageTag
        ports:
        - containerPort: 8080